---
doc_lint: off
---

# 외부 사실 확인 기록 (2026-09-04)

PRD가 인용한 외부 사실과 출처. 구현 중 현재 문서와 다르면 현재 문서를 따르고 아래 표에 "변경" 행을 추가한다.

## 데이타솔루션 브랜드

| 항목 | 값 | 출처 | 방법 |
|---|---|---|---|
| CSS 변수 `--primary` | `#133f91` | https://www.datasolution.kr (common.css `:root`) | 브라우저에서 스타일시트 규칙 열람 |
| `--primary-02` | `#00aeef` | 같음 | |
| `--blue-main`, `--blue-01…04` | `#1366e3`, `#1A73E8` `#007FFF` `#0071C5` `#0047AB` | 같음 | |
| 뉴트럴 | `#222222`(black-01) `#747474`(gray) `#cdcdcd`(light-gray) `#f7f7f7`(white-01) | 같음 | |
| 실측 보조색 | `rgb(0,43,150)`=#002B96, `#666666`, `#999999`, `#F6F7FB`, `#BBBFCB` | CI 페이지 computed style 빈도 상위 | `getComputedStyle` 집계 |
| 본문 폰트 | `Pretendard, sans-serif` | 같음 | `body` computed font-family |
| CI 색 정의 | 메인 블루 C100 · 딥블루 C100 M85 K10. "블루 = 신뢰·블루오션, 상단 하늘/하단 심연" | https://www.datasolution.kr/prcenter/ci | 페이지 본문 |
| 로고 파일 | `images/img-pr-ci-symbolmark-datasolution-01.png` 등 | 같음 | 저장소에 넣지 않음(상표) |
| 공식 브랜드 가이드·PPT 템플릿 | 미확보 | | 합류 후 확인(PRD 부록 F-1) |

## GitLab MCP 서버

| 항목 | 값 | 출처 |
|---|---|---|
| 상태·티어 | Beta. GitLab 19.2부터 Free 포함(이전 Premium/Ultimate). GitLab.com·Self-Managed·Dedicated | https://docs.gitlab.com/user/model_context_protocol/mcp_server/ |
| 엔드포인트 | `https://<gitlab>/api/v4/mcp` (HTTP 전송) | 같음 |
| Claude Code 설정 | `claude mcp add --transport http GitLab https://<gitlab>/api/v4/mcp`, 인증은 `/mcp`에서 OAuth 브라우저 승인 | 같음 |
| 프로토콜 | 2025-03-26, 2025-06-18 (GitLab 18.7에서 추가) | 같음 |
| 전제 | GitLab Duo 활성, beta 기능 허용, MCP 접근 허용. 툴 이름 접두사 32자 제한, DCR 시간당 10회 | 같음 |
| 툴 목록 | issues(create_issue, get_issue), MR(save_merge_request, get_merge_request, list_merge_requests, get_merge_request_commits/diffs/pipelines/notes, save_note, save_merge_request_review, accept_merge_request), pipelines(get_pipeline, get_pipeline_jobs, get_job, list_pipelines, save_pipeline, manage_pipeline), repo(add_commit, add_branch, list_branches, get_repository_file, get_commit, list_releases), work items(get_work_item, get_workitem_notes, link_work_items, save_work_item, list_work_items), search(search, search_labels, semantic_code_search), 기타(get_mcp_server_version, list_project_members, list_wiki_pages) | https://docs.gitlab.com/user/model_context_protocol/mcp_server_tools/ |
| 폴백 | 커뮤니티 `@zereight/mcp-gitlab`(PAT, stdio) | 일반 지식. 구현 시 최신 README 확인 |

## LiteLLM Proxy

| 항목 | 값 | 출처 |
|---|---|---|
| 버전 | v1.85.0(2026-03) 이후. `mock_testing_fallbacks` 류 요청 플래그는 프록시에서 무시됨 | https://docs.litellm.ai/docs/proxy/configs |
| config 키 | `model_list`(model_name, litellm_params, model_info, rpm, tpm), `litellm_settings`(drop_params, num_retries, request_timeout, fallbacks, context_window_fallbacks, success_callback, cache), `router_settings`(routing_strategy, model_group_alias, redis_*), `general_settings`(master_key, database_url, alerting, store_model_in_db, background_health_checks, health_check_interval), `environment_variables`, `credential_list`, `guardrails`, `mcp_servers` | 같음 + https://docs.litellm.ai/docs/mcp |
| 시크릿 참조 | `os.environ/NAME` | 같음 |
| 헬스 | `GET /health/liveliness`, `GET /health/readiness`, `GET /health`(healthy_endpoints/unhealthy_endpoints, `?model=`), `GET /health/services` | https://docs.litellm.ai/docs/proxy/health |
| 키 | `POST /key/generate`(models, team_id, max_budget, budget_duration, duration, metadata, tpm_limit, rpm_limit), `GET /key/info?key=`, `POST /key/update`, `POST /key/block`, `POST /key/unblock`, `POST /key/{key}/regenerate` | https://docs.litellm.ai/docs/proxy/virtual_keys |
| 팀·사용자 | `POST /team/new`, `GET /team/info?team_id=`, `POST /user/new`, `GET /user/info?user_id=` | 같음 |
| 비용 | `GET /spend/logs?summarize=`, `GET /global/spend/report?group_by=team\|customer\|api_key\|internal_user_id&start_date&end_date`, `GET /spend/keys`, `GET /spend/users`, `GET /user/daily/activity`, 응답 헤더 `x-litellm-response-cost` | https://docs.litellm.ai/docs/proxy/cost_tracking |
| 인증 | `Authorization: Bearer <master_key>`, 마스터 키는 `sk-`로 시작 | virtual_keys 문서 |
| MCP 게이트웨이 | 프로토콜 2025-11-25(v1.80.18부터), 키·팀별 MCP 접근 제어 | https://docs.litellm.ai/docs/mcp |

## FastMCP · 라이브러리

| 항목 | 값 | 출처 |
|---|---|---|
| FastMCP | 4.0.1(2026-09-02). MCP 2026-07-28 세션리스 프로토콜 + 구버전 협상. `@mcp.tool`, `task=True`, `Depends`. Python ≥ 3.10 | https://pypi.org/project/fastmcp/ , https://gofastmcp.com/updates |
| MCP Python SDK | v2 (2026-07-28 동시 출시) | https://jlowin.dev/blog/fastmcp-3 (FastMCP 3 소개) 및 updates |
| python-docx | 1.2.0 (2025-06-16), lxml·typing_extensions 의존 | https://pypi.org/project/python-docx/ |
| python-pptx | 1.0.x 계열 (2024 이후 1.0 안정). 구현 시 PyPI 최신 확인 | https://pypi.org/project/python-pptx/ |

## Claude Code 플러그인·스킬·훅 (문서 확인)

| 항목 | 값 | 출처 |
|---|---|---|
| plugin.json 키 | name, displayName, version, description, author, homepage, repository, license, skills(./skills/), agents, mcpServers(./.mcp.json), lspServers, hooks(hooks/hooks.json), monitors, dependencies, userConfig | https://code.claude.com/docs/en/plugins.md , plugins-reference.md |
| 플러그인 변수 | `${CLAUDE_PLUGIN_ROOT}`, `${CLAUDE_PLUGIN_DATA}`, `${CLAUDE_PROJECT_DIR}` | 같음 |
| SKILL.md frontmatter | name, description, when_to_use, argument-hint, arguments, disable-model-invocation, user-invocable, allowed-tools, disallowed-tools, model, effort, context(fork), agent, background, hooks, paths, shell, metadata, license, compatibility. 본문 500줄 이하 권장 | https://code.claude.com/docs/en/skills.md |
| 훅 이벤트 | PreToolUse, PostToolUse, Stop, SubagentStop, SessionStart, SessionEnd, UserPromptSubmit, PreCompact, Notification 등 30여 종 | https://code.claude.com/docs/en/hooks.md |
| 훅 종료 코드 | 0 = 통과(stdout JSON 해석), 2 = 차단(stderr가 이유), 기타 = 비차단 오류 | 같음 |
| 훅 JSON | Stop/PostToolUse: `{"decision":"block","reason"}`; PreToolUse: `hookSpecificOutput.permissionDecision allow\|deny\|ask`; 공통 `systemMessage` | 같음 |
| `.mcp.json` | stdio(command, args, env), http(url, headers). `${VAR}`, `${VAR:-default}` 확장 | https://code.claude.com/docs/en/mcp.md |
| CLAUDE.md import | `@path` (상대 경로는 파일 기준, 최대 4단계) | https://code.claude.com/docs/en/memory.md |

## 로컬 자산 경로 (이 PC)

- `E:\workspace\ui-skill-set` (커밋 `78b81f7`, MIT)
- `E:\workspace\품의서\.claude\skills\review-report-writer`
- `E:\workspace\weekly-report` (CLAUDE.md·AGENTS.md 스킬 분할 방식, docx 폰트 맑은 고딕 확인)
- `E:\workspace\agent-harness` (zero-dep 규약, pytest 마커)
- `E:\workspace\Pretendard-1.3.9`
